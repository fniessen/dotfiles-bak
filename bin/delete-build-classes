#!/usr/bin/env bash

# If you delete "build", you run the risk to delete folders such as
# `schema/ab-products/common/mobile/src/build'.

# Is the 'clean' target not good enough?
find . -iname "classes" | xargs rm -rf
