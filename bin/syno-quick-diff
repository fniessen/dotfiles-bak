#!/usr/bin/env bash

SERVER=192.168.0.176
USERNAME=nimda

SRC="/cygdrive/d/Users/fni/"
DEST="/volume1/Backup/XIPHIAS/D/Users/fni/"

diff <(ls -1R $SRC | sed "s/$SRC//") \
     <(ssh $USERNAME@$SERVER "ls -1R $DEST | sed \"s/$DEST//\"") \
    && echo "Directories $SRC and $DEST seem identical"
